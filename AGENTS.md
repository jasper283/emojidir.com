# Project Maintenance Rules

## Sitemap timestamps

When making a material change to an indexed page's structure, visible content, metadata, or structured data, update the corresponding timestamp in [`lib/sitemap-dates.ts`](lib/sitemap-dates.ts). Do not use the current request time as a substitute for a real content update.
